{
  "name": "zepp-api",
  "version": "1.0.0",
  "description": "Zepp device heart rate and location data API",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "redis": "^5.8.0"
  },
  "engines": {
    "node": "20.x"
  }
}
